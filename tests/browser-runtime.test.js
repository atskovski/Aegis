'use strict';const test=require('node:test');const assert=require('node:assert/strict');const {REQUIRED}=require('../src/core/engine-contract');const {createBrowserRuntime}=require('../src/core/browser-runtime');
function fake(){const calls=[];const x={name:()=> 'fake',version:()=> '1',capabilities:()=>({views:true,sessions:true,protocols:true,networkInterception:true,permissions:true,devicePermissions:true,proxy:true,downloads:true,devtoolsProtocol:true,isolatedWorlds:true,certificatePolicy:true,navigationPolicy:true,rendererLifecycle:true}),getRuntimeVersions:()=>({})};for(const k of REQUIRED)if(!x[k])x[k]=(...a)=>{calls.push([k,...a]);return k};x.calls=calls;return x;}
test('runtime delegates privileged operations only through adapter',()=>{const e=fake(),r=createBrowserRuntime(e);assert.equal(r.view({sandbox:true}),'createView');assert.equal(r.evaluate({},'1',false),'executeJavaScript');assert.equal(e.calls.length,2);});
